
для работы тем в шаблонах нужно вручную поотключать установку тем в их конфигурационных файлах:

  ИМЯ_ШАБЛОНА/settings/config/config.php


удалить строки подобные этой:

$config['view']['theme'] = 'default';

т.е. нельзя присваивать тему в конфиге т.к. она перекроет (!) значение из админки
